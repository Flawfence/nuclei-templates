id: internal-hostname-disclosure

info:
  name: Internal Hostname / AD FQDN Disclosure
  author: Flawfence
  severity: low
  description: |
    The application discloses an internal, non-public host name - for example an
    Active Directory / corporate FQDN (such as HOST.EMEA.EXAMPLE.GRP) or an internal
    zone ending in .local/.corp/.internal/.intranet/.lan - inside a redirect Location
    header or the response body. Leaking internal host names reveals network topology
    and assists an attacker with lateral movement and social engineering.
  reference:
    - https://cwe.mitre.org/data/definitions/200.html
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N
    cvss-score: 5.3
    cwe-id: CWE-200
  metadata:
    max-request: 1
  tags: exposure,disclosure,misconfig,info-leak

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: or
    matchers:
      # Redirect to an internal-only DNS suffix (never a public TLD -> low FP).
      - type: regex
        part: header
        name: internal-fqdn-redirect
        regex:
          - '(?i)location:\s*https?://[a-z0-9-]+(?:\.[a-z0-9-]+)*\.(?:grp|local|corp|internal|intranet|lan)(?::\d+)?[/\s]'

      # Same internal-only suffix leaked anywhere in the body.
      - type: regex
        part: body
        name: internal-fqdn-body
        regex:
          - '(?i)https?://[a-z0-9-]+(?:\.[a-z0-9-]+)*\.(?:grp|local|corp|internal|intranet|lan)(?::\d+)?[/"'']'

    extractors:
      - type: regex
        part: response
        regex:
          - '(?i)[a-z0-9-]+(?:\.[a-z0-9-]+)+\.(?:grp|local|corp|internal|intranet|lan)\b'
